meta {
  name: Trigger Satellite dimensions/mass import from ESA Discos
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v1/satellites/esa-integration
  body: none
  auth: apikey
}

headers {
  Accept: application/json
}

auth:apikey {
  key: Authorization
  value: asdasd
  placement: header
}

tests {
  // Also not triggering this ingest - instead check partial auth broken
  test("Status code 401 (with wrong/partial auth)", function() {
      expect(res.getStatus()).to.equal(401);
  });
}

docs {
  ## Description
  Starts Satellite data ingestion process by querying ESA Discos API.
  
  ## User Roles
  |User Role       |Permissions                                                      |
  |----------------|-----------------------------------------------------------------|
  |Superuser       |Can trigger integration.                                         |
  |Everyone else   |Can't trigger integration.                                       |
}
