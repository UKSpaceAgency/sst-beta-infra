meta {
  name: Returns the last ingested SpaceTrack CDM
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/v1/cdms/latest
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

tests {
  const resp = res.getBody();
  
  const expected_keys = [
      "id",
      "createdAt",
      "updatedAt",
      "externalId",
      "eventId"
  ];
  
  test("Status code 200", function() {
      expect(res.getStatus()).to.equal(200);
  });
  
  test("Latest CDM returned contains expected fields", () => {
      expect(resp).to.have.all.keys(expected_keys);
  });
}
