meta {
  name: FOR TEST - Get Analysis ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/events/:event_id/summary?limit=10&offset=0&sort_order=asc
  body: none
  auth: bearer
}

params:query {
  limit: 10
  offset: 0
  sort_order: asc
}

params:path {
  event_id: {{testAnalysisEventShortId}}
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{bearerToken}}
}

tests {
  // run post request to log the long ID for next request
  const new_long_id = res.getBody()[res.getBody().length - 1].id;
  
  bru.setVar("testAnalysisAnalysisId", new_long_id);
}

docs {
  ## Description
  Returns summary data for an Event. Data can be taken from Analysis or from CDMs
  (the most recent type is returned).
  
  ## User Roles
  |User Role       |Permissions                                                         |
  |----------------|--------------------------------------------------------------------|
  |Superuser       |Can retrieve summaries for all events.                              |
  |Analyst         |Can retrieve summaries for all events.                              |
  |Everyone else   |Can retrieve summaries for events concerning user's organization.   |
}
